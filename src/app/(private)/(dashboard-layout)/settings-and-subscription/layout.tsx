export const metadata = {
  title: 'Settings and Subscription',
 description: '',
};

const SettingsAndSubscriptionLayout = async ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
}

export default SettingsAndSubscriptionLayout;
