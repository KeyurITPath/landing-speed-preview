export const metadata = {
  title: 'Refund Policy',
  description: '',
};

const RefundPolicyLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default RefundPolicyLayout;
