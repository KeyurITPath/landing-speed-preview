export const metadata = {
  title: 'Terms of Service',
  description: '',
};

const TermsOfServiceLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default TermsOfServiceLayout;
