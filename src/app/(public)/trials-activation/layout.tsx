export const metadata = {
  title: 'Trial Activation',
  description: '',
};

const TrialActivationLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default TrialActivationLayout;
